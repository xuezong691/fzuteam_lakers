按照readme配置后端进行测试

检查并修改 my_backend.py 的 db_config（如果你的 MySQL 密码或用户名不同）这一步一定要修改为自己的mysql密码和用户名

如果环境都有，并且已经导入了db_schema.sql到自己的mysql里，
在v2文件夹内打开cmd运行下面的代码应该也可以打开服务端：
python -m venv venv
venv\Scripts\activate
python my_backend.py

这份文件同级目录下文件夹内的版本是我测试过能正常运行的版本，除了上述的密码被我改回

这个版本内的my_backend.py和my_dify_api.py是经过修改的，只用于测试时使用
